//Going over local and global variables
//Local = declared inside a method
//Global = declared inside a class

public class Main {
    public static void main(String[] args) {
        DiceRoller diceRoller = new DiceRoller();
    }
}